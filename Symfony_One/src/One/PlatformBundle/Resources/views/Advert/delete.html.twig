{% extends "OnePlatformBundle::ensemble.html.twig" %}

{% block bodyY %}
  <section id="add" class="container content-section text-center" >
      <h3>Supprimer plat</h3>
      <p>
         SUpprimer le plat "{{ advert.title }}" ?
      </p>
      <form action="{{ path('One_platform_delete', {'id': advert.id}) }}" method="post">
          <a href="{{ path('One_platform_view', {'id': advert.id}) }}" class="btn btn-default">
          <i class="glyphicon glyphicon-chevron-left"></i>
          Retour à la recette
          </a>
          <input type="submit" value="Supprimer" class="btn btn-danger" />
          {{ form_rest(form) }}
      </form>
  </section>
{% endblock %}